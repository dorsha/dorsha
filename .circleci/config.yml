version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - restore_cache:
          key: npm-dorsha-{{ checksum "package.json" }}
      - run:
          name: Install dependencies
          command: |
              sudo npm install npm@latest -g
              npm install -g bower grunt-cli
              npm install
              bower install
      - save_cache:
          key: npm-dorsha-{{ checksum "package.json" }}
          paths:
            - node_modules
      - run:
          name: Build
          command: grunt build
      - run:
          name: Test
          command: |
              npm run lint
              npm run coverage
      - run:
          name: Deploy
          command: |
              echo CircleCI tag is: $CIRCLE_TAG
              if [[ $CIRCLE_TAG =~ master ]]; then
                ./node_modules/.bin/firebase deploy --token $FIREBASE_TOKEN
              else
                echo 'Nothing to do.'
              fi
